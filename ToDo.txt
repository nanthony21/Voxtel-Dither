New Features:
Create option to  ‘Correct’ or ‘create’ entered abberation
display total OPD. in terms of waves and actual distance

Bugs:
Fix freeze when N2<N1
Instead of minimal layers it should be optimal layers
make the height field automatically update with the number of optimal layers.

Delete functionality for "packed" layers

Is composite broken?

use new N values

create text display window to display errors as well as other printouts such as task progress indications

Speed up 3d plotting

only create images on demand to save ram.